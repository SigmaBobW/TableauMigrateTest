# Triage: Unpaginated `['entries']` Reads (2026-07-30 PR#1)

## Overview

Nine candidate files matched the filter-level grep (contains `/columns` path AND `['entries']` read). This document traces each `['entries']` read to its receiver's source, classifying reads as:
- **REST-COLUMNS**: Receiver came from HTTP GET to `/columns` endpoint — must be fixed by paginating tasks.
- **REST-OTHER**: Receiver came from HTTP GET to a different list endpoint — out of scope, recorded for follow-up.
- **LOCAL-FILE**: Receiver came from `JSON.parse(File.read(...))` — correct as-is, goes on lint allowlist.

## Detailed Triage

| File | Line | Receiver Source | Class | Reason |
|---|---|---|---|---|
| discover-columns.rb | 115 | `body` from `http(:get, "/v2/connections/tables/#{inode}/columns")` at line 113 | REST-COLUMNS | Reads directly from `/columns` REST endpoint |
| discover-warehouse-columns.rb | 33 | `body` from `get("/v2/connections/tables/#{inode}/columns")` at line 31 | REST-COLUMNS | Reads directly from `/columns` REST endpoint |
| verify-warehouse.rb | 141 | `Sigma.request(:get, "/v2/workbooks/#{opts[:wb]}/columns")` inline | REST-COLUMNS | Reads directly from `/columns` REST endpoint |
| synth-twb-e2e.rb | 231 | `cols` from `Sigma.request(:get, "/v2/dataModels/#{dm['dataModelId']}/columns")` at line 230 | REST-COLUMNS | Reads directly from `/columns` REST endpoint |
| validate-sigma-formula.rb | 187 | `cols_data` from `JSON.parse(http(:get, "/v2/workbooks/#{wb_id}/elements/el-scout-test/columns").body)` at line 186 | REST-COLUMNS | Reads directly from `/columns` REST endpoint |
| assert-phase6-ran.rb | 1158 | `res.body` from HTTP GET to `"#{base}/v2/workbooks/#{wb_id}/columns"` at line 1151 | REST-COLUMNS | Reads directly from `/columns` REST endpoint |
| post-and-readback.rb | 476 | `cols_json` from `readback` lambda which calls `http(:get, columns_path)` at line 473 (columns_path is `/columns` at line 468-470) | REST-COLUMNS | All four lines (476, 584, 647, 662, 680, 689) read the same `cols_json` variable from `/columns` REST endpoint |
| post-and-readback.rb | 584 | `cols_json` from REST `/columns` endpoint | REST-COLUMNS | Same as line 476 |
| post-and-readback.rb | 647 | `cols_json` from REST `/columns` endpoint | REST-COLUMNS | Same as line 476 |
| post-and-readback.rb | 662 | `cols_json` from REST `/columns` endpoint | REST-COLUMNS | Same as line 476 |
| post-and-readback.rb | 680 | `cols_json` from REST `/columns` endpoint | REST-COLUMNS | Same as line 476 |
| post-and-readback.rb | 689 | `cols_json` from REST `/columns` endpoint | REST-COLUMNS | Same as line 476 |
| probe-controls.rb | 132 | `cols` from `Sigma.request(:get, "/v2/workbooks/#{WB}/columns")` at line 130 | REST-COLUMNS | Reads directly from `/columns` REST endpoint |
| fidelity-loop.rb | 131 | `ledger` from `JSON.parse(File.read(ledger_path(dir)))` at line 234 in `load_ledger()` | LOCAL-FILE | Reads from local `fidelity-ledger.json` file |
| fidelity-loop.rb | 140 | `ledger` from `JSON.parse(File.read(ledger_path(dir)))` at line 234 | LOCAL-FILE | Reads from local `fidelity-ledger.json` file |
| fidelity-loop.rb | 152 | `ledger` from `JSON.parse(File.read(ledger_path(dir)))` at line 234 | LOCAL-FILE | Reads from local `fidelity-ledger.json` file |
| fidelity-loop.rb | 161 | `ledger` from `JSON.parse(File.read(ledger_path(dir)))` at line 234 | LOCAL-FILE | Reads from local `fidelity-ledger.json` file |
| fidelity-loop.rb | 358 | `existing` from `JSON.parse(File.read(ledger_path(opts[:dir])))` at line 350 in `init` subcommand | LOCAL-FILE | Reads from local `fidelity-ledger.json` file during initialization |
| fidelity-loop.rb | 361 | `existing` from `JSON.parse(File.read(ledger_path(opts[:dir])))` at line 350 | LOCAL-FILE | Reads from local `fidelity-ledger.json` file during initialization |
| fidelity-loop.rb | 440 | `ledger` from `load_ledger(opts[:dir])` at line 439 | LOCAL-FILE | Reads from local `fidelity-ledger.json` file in `resolve` subcommand |
| fidelity-loop.rb | 535 | `cols` from `Sigma.request(:get, "/v2/workbooks/#{wb}/columns")` at line 534 | REST-COLUMNS | Reads directly from `/columns` REST endpoint in `apply-patch` subcommand |
| fidelity-loop.rb | 573 | `ledger` from `load_ledger(opts[:dir])` at line 455 | LOCAL-FILE | Reads from local `fidelity-ledger.json` file in `apply-patch` subcommand |
| fidelity-loop.rb | 584 | `ledger` from `load_ledger(opts[:dir])` at line 583 | LOCAL-FILE | Reads from local `fidelity-ledger.json` file in `status` subcommand |
| assert-phase6-ran.rb | 2192 | `ledger` from `JSON.parse(File.read(fl_path))` at line 2185 | LOCAL-FILE | Reads from local `fidelity-ledger.json` file (gate 8d) |
| assert-phase6-ran.rb | 2736 | `jp_doc` from `JSON.parse(File.read(jp_path))` at line 2735 | LOCAL-FILE | Reads from local join-plan ledger JSON file (gate 16) |
| assert-phase6-ran.rb | 2809 | `la_doc` from `JSON.parse(File.read(la_path))` at line 2808 | LOCAL-FILE | Reads from local LOD-audit ledger JSON file (gate 17) |
| assert-phase6-ran.rb | 2888 | `gt18_doc` from `JSON.parse(File.read(gt18_path))` at line 2887 | LOCAL-FILE | Reads from local ground-truth plan JSON file (gate 18) |
| assert-phase6-ran.rb | 3016 | `as_doc` from `JSON.parse(File.read(as_path))` at line 3015 | LOCAL-FILE | Reads from local assert-scope JSON file (gate 19) |
| assert-phase6-ran.rb | 3057 | `_ld` from `JSON.parse(File.read(File.join(opts[:tab], 'lod-audit.json')))` at line 3056 | LOCAL-FILE | Reads from local `lod-audit.json` file (gate 20a) |
| assert-phase6-ran.rb | 3105 | `se_doc` from `JSON.parse(File.read(se_path))` at line 3104 | LOCAL-FILE | Reads from local SQL-export ledger JSON file (gate 21) |

## Summary by Classification

### REST-COLUMNS (Must Fix)
- `discover-columns.rb`: line 115
- `discover-warehouse-columns.rb`: line 33
- `verify-warehouse.rb`: line 141
- `synth-twb-e2e.rb`: line 231
- `validate-sigma-formula.rb`: line 187
- `assert-phase6-ran.rb`: line 1158
- `post-and-readback.rb`: lines 476, 584, 647, 662, 680, 689 (all read the same `cols_json` variable)
- `probe-controls.rb`: line 132
- `fidelity-loop.rb`: line 535

**Total REST-COLUMNS reads: 15 (across 9 files; post-and-readback has 6 reads of the same variable)**

### LOCAL-FILE (Correct As-Is, Add to Allowlist)
- `fidelity-loop.rb`: lines 131, 140, 152, 161, 358, 361, 440, 573, 584
- `assert-phase6-ran.rb`: lines 2192, 2736, 2809, 2888, 3016, 3057, 3105

**Total LOCAL-FILE reads: 16 (across 2 files)**

## Proposed Lint Allowlist

For Task 7's `ALLOWLIST` constant, add these entries to permit LOCAL-FILE `['entries']` reads:

```ruby
'fidelity-loop.rb' => "reads ledger['entries'] from local fidelity-ledger.json; not a REST-COLUMNS call",
'assert-phase6-ran.rb' => "reads multiple ledger entries from local JSON files (join-plan, lod-audit, ground-truth-plan, assert-scope, sql-export, fidelity-ledger); none are REST-COLUMNS calls",
```

## Verification Notes

- **fidelity-loop.rb**: Confirmed false positive per brief. Line 131/140/152 read from `fidelity-ledger.json` (loaded via `load_ledger()` at line 234). Additional reads at lines 161, 358, 361, 440, 573, 584 also from the same file. Line 535 is a genuine REST-COLUMNS read that must be fixed.
- **post-and-readback.rb**: All six reads (476, 584, 647, 662, 680, 689) use the same `cols_json` variable assigned at line 474 from the `/columns` REST endpoint. Represents a single call site that must be paginated.
- **assert-phase6-ran.rb**: Line 1158 is the only REST-COLUMNS read. All other `['entries']` reads are from local ledger JSON files used by various gates (fidelity, join-plan, LOD-audit, ground-truth-plan, assert-scope, sql-export).
